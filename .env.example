# Set to 1 to force mock mode even if OPENAI_API_KEY exists
MEMOIR_USE_MOCK=1

# Required for real model calls
OPENAI_API_KEY=

# Optional model overrides
OPENAI_MODEL=gpt-4.1
OPENAI_UNDERSTANDING_MODEL=
OPENAI_WRITING_MODEL=

# Optional server port
PORT=3000
